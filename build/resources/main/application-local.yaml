spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/shop
    driver-class-name: com.mysql.cj.jdbc.Driver
  config:
    activate:
      on-profile: local
  jpa:
    show-sql: true

server:
  port: 8080
